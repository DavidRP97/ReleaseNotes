@model IEnumerable<ReleasePDVViewModel>

@{
    ViewData["Title"] = "Index";
}

<div class="col-lg-12">
    <div class="card">
        <div class="card-header">
            <h4>PDV</h4>
        </div>
        <div class="card-body">
            <p class="text-muted m-b-15">
                Notas de Versões
            </p>

            <ul class="list-unstyled">
                @foreach (var item in Model)
                {
                    <li>
                        <a asp-action="Modules" asp-route-id="@item.ReleaseId" asp-controller="ReleasePDV"><b>Versão: </b>@item.VersionNumber - @item.VersionDate</a>
                    </li>
                }
            </ul>

        </div>
    </div>
</div>