@model IEnumerable<FeedbackViewModel>

@if (User.IsInRole(@Role.SuperControleAdmin))
{
    <partial name = "_LastFeedbacks" />
}
@if(User.IsInRole(@Role.Client))
{
    <partial name = "_LastFeedbacksClients" />
}
